export const passDay = (population) => {
    for (let i = 0; i < population.length; i++) {
        for (let j = 0; j < population[i].length; j++) {
            if (population[i][j].type === 1) {
                let mI = i + 1;
                let mJ = j + 1;

            }
        }
    }
}